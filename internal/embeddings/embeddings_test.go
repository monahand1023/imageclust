package embeddings

import "testing"

// GetEmbedding requires a live CLIP model and is covered by integration tests.
// Set CLIP_MODEL_PATH to the model file to enable those tests.

func TestPackageCompiles(t *testing.T) {
	// Verifies the package builds without GoCV or AWS dependencies.
}
